var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/lib/hooks/create-shared-composable.ts
var create_shared_composable_exports = {};
__export(create_shared_composable_exports, {
  createSharedComposable: () => createSharedComposable
});
module.exports = __toCommonJS(create_shared_composable_exports);
var import_vue2 = require("vue");

// src/lib/hooks/try-on-scope-dispose.ts
var import_vue = require("vue");
function tryOnScopeDispose(fn) {
  if ((0, import_vue.getCurrentScope)()) {
    (0, import_vue.onScopeDispose)(fn);
    return true;
  }
  return false;
}

// src/lib/hooks/create-shared-composable.ts
function createSharedComposable(composable) {
  let subscribers = 0;
  let state;
  let scope;
  const dispose = () => {
    subscribers -= 1;
    if (scope && subscribers <= 0) {
      scope.stop();
      state = void 0;
      scope = void 0;
    }
  };
  return (...args) => {
    subscribers += 1;
    if (!state) {
      scope = (0, import_vue2.effectScope)(true);
      state = scope.run(() => composable(...args));
    }
    tryOnScopeDispose(dispose);
    return state;
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createSharedComposable
});
