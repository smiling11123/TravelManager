import { Fn } from './shared.js';
import 'vue';

/**
 * Call onScopeDispose() if it's inside an effect scope lifecycle, if not, do nothing
 *
 * @param fn
 */
declare function tryOnScopeDispose(fn: Fn): boolean;

export { tryOnScopeDispose };
