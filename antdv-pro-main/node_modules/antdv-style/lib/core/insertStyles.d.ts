import type { EmotionCache } from '@emotion/css/create-instance';
import type { SerializedStyles } from '@emotion/serialize';
import type { ClassNameGeneratorOption } from '../types';
export declare function createHashStyleName(cacheKey: string, hash: string): string;
/**
 * 向浏览器插入样式表
 * @param cache
 * @param serialized
 * @param isStringTag
 * @param options
 */
export declare function insertStyles(cache: EmotionCache, serialized: SerializedStyles, isStringTag: boolean, options: ClassNameGeneratorOption): string | undefined;
