import type { StyledConfig } from '../types';
/**
 * 创建一个 styled api 的 ThemeProvider
 *  如果用户有设定 ThemeProvider，就使用用户的，否则使用 ThemeContext.Provider
 * @param styledConfig
 */
export declare function createStyledThemeProvider(styledConfig: StyledConfig): import("../types").StyledThemeProvider | ((props: {
    theme: any;
} & {}) => any);
