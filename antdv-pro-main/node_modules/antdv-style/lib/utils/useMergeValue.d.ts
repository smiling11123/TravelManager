import type { Ref } from 'vue';
export declare function useMergeValue<T>(defaultStateValue: T, options: {
    value: Ref<T>;
    defaultValue: T;
    onChange: (v: T) => void;
}): [Ref<T>, (v: T) => void];
