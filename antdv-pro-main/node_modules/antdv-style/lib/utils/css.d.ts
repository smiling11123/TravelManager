import type { RegisteredCache } from '@emotion/utils';
/**
 * 判断是否是 ReactCss 的编译产物
 * @param params
 */
export declare function isReactCssResult(params: any): boolean;
export declare function classnames(args: any): string;
export declare function mergeCSS(registered: RegisteredCache, css: any, className: string): string;
