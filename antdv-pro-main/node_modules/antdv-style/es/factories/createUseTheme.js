import { computed as o, reactive as h } from "vue";
import { useContext as t } from "../utils/context.js";
import "@emotion/css";
import "@emotion/serialize";
import "@emotion/utils";
import "@emotion/css/create-instance";
import { useThemeMode as f } from "../hooks/useThemeMode.js";
import "ant-design-vue";
import { useAntdTheme as C } from "../hooks/useAntdTheme.js";
import { DEFAULT_THEME_CONTEXT as l } from "../functions/setupStyled.js";
function O(m) {
  return () => {
    const { StyleEngineContext: r } = m, { StyledThemeContext: n, CustomThemeContext: i, prefixCls: s } = t(r), p = C(), u = f(), T = t(i), e = t(n ?? l) || {}, c = o(() => ({
      ...p.value,
      ...h(u),
      ...T,
      prefixCls: s || "ant"
    }));
    return !e || Object.keys(e).length === 0 ? c : o(() => e);
  };
}
export {
  O as createUseTheme
};
