import type { StyledThemeProvider } from '../../types';
import type { ThemeProviderProps } from './type';
interface TokenContainerProps<T, S = Record<string, string>> extends Pick<ThemeProviderProps<T, S>, 'customToken' | 'customStylish' | 'prefixCls'> {
    StyledThemeProvider: StyledThemeProvider;
    defaultCustomToken?: ThemeProviderProps<T, S>['customToken'];
}
declare const TokenContainer: (props: TokenContainerProps<any, Record<string, string>> & {}) => any;
export default TokenContainer;
