import type { ComputedRef } from 'vue';
import type { BaseReturnType, ClassNameGeneratorOption, HashPriority, ReturnStyleToUse, Theme } from '../../types';
import type { Emotion } from '../../core';
import type { Context } from '../../utils/context';
import type { StyleOrGetStyleFn } from './types.ts';
interface CreateStylesFactory {
    EmotionContext: Context<Emotion>;
    hashPriority?: HashPriority;
    useTheme: () => ComputedRef<Theme>;
}
/**
 * 创建样式基础写法
 */
export declare function createStylesFactory({ hashPriority, useTheme, EmotionContext }: CreateStylesFactory): <Props, Input extends BaseReturnType = BaseReturnType>(styleOrGetStyle: StyleOrGetStyleFn<Input, Props>, options?: ClassNameGeneratorOption) => (props?: Props | undefined) => {
    styles: ComputedRef<ReturnStyleToUse<Input>>;
    cx: import("../../types").ClassNamesUtil;
    theme: ComputedRef<Theme>;
    prefixCls: ComputedRef<string>;
};
export {};
