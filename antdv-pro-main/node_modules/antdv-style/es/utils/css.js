import { getRegisteredStyles as s } from "@emotion/utils";
function f(n) {
  return typeof n == "object" && "styles" in n && "name" in n && "toString" in n;
}
function l(n) {
  let r = "";
  for (let i = 0; i < n.length; i++) {
    const e = n[i];
    if (e === null)
      continue;
    let t;
    switch (typeof e) {
      case "boolean":
        break;
      case "object": {
        if (Array.isArray(e))
          t = l(e);
        else {
          t = "";
          for (const o in e)
            e[o] && o && (t && (t += " "), t += o);
        }
        break;
      }
      default:
        t = e;
    }
    t && (r && (r += " "), r += t);
  }
  return r;
}
function u(n, r, i) {
  const e = [], t = s(
    n,
    e,
    i
  );
  return e.length < 2 ? i : t + r(e);
}
export {
  l as classnames,
  f as isReactCssResult,
  u as mergeCSS
};
