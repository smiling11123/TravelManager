import { registerStyles as m } from "@emotion/utils";
const i = typeof document < "u";
function u(t, e) {
  return `${t}-${e}`;
}
function a(t, e, h, y) {
  const f = y.hashPriority || "high";
  m(t, e, h);
  const r = `.${u(t.key, e.name)}`, l = f === "low" ? `:where(${r})` : r;
  if (t.inserted[e.name] === void 0) {
    let o = "", n = e;
    do {
      const s = t.insert(
        e === n ? l : "",
        n,
        t.sheet,
        !0
      );
      !i && s !== void 0 && (o += s), n = n.next;
    } while (n !== void 0);
    if (!i && o.length !== 0)
      return o;
  }
}
export {
  u as createHashStyleName,
  a as insertStyles
};
