import type { EmotionCache } from '@emotion/css/create-instance';
import type { ClassNameGeneratorOption, ClassNamesUtil } from '../types';
/**
 * CSS相关方法生成器 用于序列化的样式转换生成 className
 * @param cache
 * @param options
 */
export declare function createCSS(cache: EmotionCache, options: ClassNameGeneratorOption): {
    css: {
        (template: TemplateStringsArray, ...args: import("@emotion/serialize").CSSInterpolation[]): string;
        (...args: import("@emotion/serialize").CSSInterpolation[]): string;
    };
    cx: ClassNamesUtil;
};
