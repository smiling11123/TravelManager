import { computed as e } from "vue";
import "@emotion/css";
import "@emotion/serialize";
import "@emotion/utils";
import "@emotion/css/create-instance";
import { serializeCSS as i } from "../core/createSerializeStyles.js";
import { createAntdStylish as m } from "../stylish/button.js";
import { convertStylishToString as p } from "../utils/convertStylish.js";
import { useThemeMode as n } from "./useThemeMode.js";
import { useAntdToken as a } from "./useAntdToken.js";
function y() {
  const o = a(), { appearance: r, isDarkMode: t } = n();
  return e(
    () => p(
      m({
        token: o.value,
        css: i,
        appearance: r.value,
        isDarkMode: t.value
      })
    )
  );
}
export {
  y as useAntdStylish
};
