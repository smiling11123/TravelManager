import type { StyledConfig, Theme } from '../types';
export declare const DEFAULT_THEME_PROVIDER: any;
export declare let DEFAULT_THEME_CONTEXT: import("../utils/context.ts").Context<Theme>;
export declare function setupStyled(config: StyledConfig): void;
